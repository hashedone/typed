let I = fn(x) { x };
let S = fn(n, f, x) {
  f(n(f, x))
};

let zero = fn(f, x) { x };
let one = S(zero);
let two = S(one);
let three = S(two);
let four = S(three);

let plus = fn(n, m) {
  m(S, n)
};

I
